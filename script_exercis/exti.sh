# 9.Write a shell script that displays,"This script will exit with 0 exit status."Be sure that the script the script does indeed exit with a 0 exit status.

#!/bi/bash

echo "This script will exit with 0 exit status."
exit 0
